services:

    web:
        build:
            context: .
            platforms:
                - linux/amd64
        image: wodby/nginx
        container_name: "fallback_frontend"
        restart: always
        volumes:
            - ./index.html:/var/www/html/index.html
        ports:
            - 80:80
