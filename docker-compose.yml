services:

    web:
        build:
            context: .
            platforms:
                - linux/amd64
        container_name: "fallback_frontend"
        restart: always
        ports:
            - 80:80
